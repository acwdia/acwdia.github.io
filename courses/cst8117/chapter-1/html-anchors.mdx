---
title: HTML Anchors
---

import Youtube from '@site/src/components/Youtube';

# HTML Anchors

<Youtube src="https://www.youtube.com/embed/HyCjbxRm08k?rel=0&controls=1&modestbranding=1">
  <small>This YouTube video was created by <a href="https://www.youtube.com/channel/UCTBGXCJHORQjivtgtMsmkAQ">Steve Griffith</a>.</small>
</Youtube> 

## The `<a>` Element

[HTML Anchors](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/a) or 'links' are the powerhouse of the web. It's how we link to everything. Anchors can link to other websites, specific pages, photos, documents or any other resource available on the web or our web server.

- For creating a link on our webpage we use the anchor tag `<a></a>`.
- The anchor tag is a paired tag and the content we want to display as a link is added between the opening `<a>` and the closing `</a>`.
- For providing the location where the anchor tag will link we use the `href` attribute.

Example:

```html
When in doubt, <a href="https://google.ca">Google</a> it out!
```

<iframe height="300" style={{width: '100%'}} scrolling="no" title="HTML Links - Basic" src="https://codepen.io/IMD/embed/rNoMOmY?default-tab=result" frameborder="no" loading="lazy" allowtransparency="true" allowfullscreen="true">
  See the Pen <a href="https://codepen.io/IMD/pen/rNoMOmY">
  HTML Links - Basic</a> by IMD (<a href="https://codepen.io/IMD">@IMD</a>)
  on <a href="https://codepen.io">CodePen</a>.
</iframe>

Notice that the word Google is a link that takes us to the link provided in the href attribute. The `href` attribute will take a path, either absolute or relative, to the resource.

When we are working in our own file structure it's much more future-friendly working with relative paths. The URL might not always be the same and some sites could have references to a URL that doesn't exist. A relative path would be the same whatever the domain is.

## Opening links in a new Tab

When adding links to external websites, social media or documents like PDFs it is better to open the link in a new tab so that the users do not move away from our website.

We can achieve this by adding another attribute to the `<a></a>` tag. The `target="_blank"` attribute will open the URL in a new tab.

Example:

```html
When in doubt, <a href="https://google.ca" target="_blank">Google</a> it out!
```

<iframe height="300" style={{width: '100%'}} scrolling="no" title="HTML Links - New Tab" src="https://codepen.io/IMD/embed/JjwRYJB?default-tab=result" frameborder="no" loading="lazy" allowtransparency="true" allowfullscreen="true">
  See the Pen <a href="https://codepen.io/IMD/pen/JjwRYJB">
  HTML Links - New Tab</a> by IMD (<a href="https://codepen.io/IMD">@IMD</a>)
  on <a href="https://codepen.io">CodePen</a>.
</iframe>

Notice when clicking on the above link, it opens in a new tab.

> #### Note
> There must be a space between the tag name and attribute name and between an attribute value and the next attribute name.


## On Page Links

Sometimes we need to add links that will take us to the different parts of the same page. Most commonly used with FAQs, Back to Top links etc.

The on-page links work in two parts:

- The link points to an element on the page
- The element being pointed at has an `id` attribute

Example:

```html{1,14}
<a href="#cakes">Jump to cakes menu</a>
<p>
  Lorem ipsum dolor sit, amet consectetur adipisicing elit. Nobis at,
  dignissimos ratione doloremque rerum non rem cum laudantium quia consectetur,
  saepe perferendis aperiam iste reprehenderit fugit ipsa, excepturi impedit.
  Saepe.
</p>
<p>
  Lorem ipsum dolor sit, amet consectetur adipisicing elit. Nobis at,
  dignissimos ratione doloremque rerum non rem cum laudantium quia consectetur,
  saepe perferendis aperiam iste reprehenderit fugit ipsa, excepturi impedit.
  Saepe.
</p>
<ul id="cakes">
  <li>Red Velvet</li>
  <li>Tiramisu</li>
  <li>Black Forest</li>
</ul>
```

<iframe height="265" style={{width: '100%'}} scrolling="no" title="&lt;a&gt;" src="//codepen.io/IMD/embed/jrEewV/?height=265&theme-id=47863&default-tab=html,result" frameborder="no" allowtransparency="true" allowfullscreen="true">
  See the Pen <a href='https://codepen.io/IMD/pen/jrEewV/'>&lt;a&gt;</a> by IMD
  (<a href='https://codepen.io/IMD'>@IMD</a>) on <a href='https://codepen.io'>CodePen</a>.
</iframe>

